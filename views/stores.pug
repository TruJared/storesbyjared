extends layout

include mixins/_storeCard
include mixins/_pagination

block content
  .inner
    h2= title
    //- acts as type of console.log on the page (see (see helpers.js)
    //- pre= h.dump(stores)
    .stores
      each store in stores
        +storeCard(store)
    +pagination(page, pages, count)
